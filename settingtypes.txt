# List of protected spawnpoints, separated by semicolons (e.g., "0,125,1.5; 50,120,-30")
protected_spawnpoints	(Protected spawnpoints)	string	""

# Horizontal distance in meters within which configured Nodes are blocked
spawn_buffer_distance	(Buffer distance from spawnpoints)	int	100

# Allow placing lava within protected zone
allow_lava	(Allow lava near spawnpoints)	bool	false

# Allow placing fire within protected zone
allow_fire	(Allow fire near spawnpoints)	bool	false

# Allow placing TNT within protected zone
allow_tnt	(Allow TNT near spawnpoints)	bool	false
